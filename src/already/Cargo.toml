[package]
name = "already"
version = "0.2.0"
edition = "2024"

[dependencies]
already_core = { workspace = true }
already_proc_macro = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
dircpy = { workspace = true }

[lints.clippy]
needless_return = "allow"
match_like_matches_macro = "allow"