# TODO - check that lib and bin together work even without CARGO_TARGET_DIR env var

[package]
name = "lib_bin_together"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
code_reload = { path = "../.." }

[workspace]

[[bin]]
name = "main"
path = "bin/main.rs"