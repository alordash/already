# TODO - write in docs that for separate lib to work you need to specify CARGO_TARGET_DIR env var so that
# generated dynamic library file will be placed near bin

[workspace]
resolver = "3"
members = ["lib_separate", "bin_separate"]

[workspace.dependencies]
code_reload = { path = "../.." }